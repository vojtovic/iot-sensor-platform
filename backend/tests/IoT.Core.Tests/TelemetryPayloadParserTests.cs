using Xunit;

namespace IoT.Core.Tests;

// Placeholder: TelemetryPayloadParser.Parse je zatím jen seam (holá kostra).
// TODO (autor): po implementaci parseru sem doplň reálné testy — validní payload,
// chybné vstupy, dedup. Skip drží testovací projekt zelený.
public class TelemetryPayloadParserTests
{
    [Fact(Skip = "TODO (autor): implementovat TelemetryPayloadParser.Parse a jeho testy")]
    public void Parse_Todo()
    {
    }
}
